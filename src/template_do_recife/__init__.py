"""template-do-recife workspace root package.

This package exists so the root project is installable and its
``[project.scripts]`` entry points (kickoff_embedder, kickoff_crew,
kickoff_flow) get registered by uv. The actual implementations live in the
workspace member packages under ``crews/``.
"""
